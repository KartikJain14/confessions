<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Confession</title>
    <!-- Import Google Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', 'Segoe UI', sans-serif;
            background-image: url('/gradient.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            color: #FFFFFF;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 40px 20px;
            box-sizing: border-box;
        }

        .edit-wrapper {
            width: 100%;
            max-width: 480px;
        }

        h1 {
            color: #ffffff;
            text-align: center;
            font-size: 1.8rem;
            margin-bottom: 30px;
            font-weight: 700;
            letter-spacing: -0.02em;
            font-family: 'Space Grotesk', sans-serif;
        }

        form {
            width: 100%;
            padding: 40px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            box-shadow: 
                0 20px 60px rgba(0, 0, 0, 0.15),
                0 8px 32px rgba(0, 0, 0, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            box-sizing: border-box;
        }

        label {
            display: block;
            margin: 15px 0 8px;
            color: #000000;
            font-weight: 600;
            font-size: 0.95rem;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 15px;
            border: 2px solid rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            margin-bottom: 10px;
            font-size: 1rem;
            font-family: inherit;
            background: rgba(255, 255, 255, 0.5);
            color: #000000;
            transition: all 0.3s ease;
            outline: none;
            box-sizing: border-box;
        }

        input[type="text"]:focus,
        input[type="number"]:focus {
            border-color: rgba(0, 0, 0, 0.3);
            background: rgba(255, 255, 255, 0.8);
        }

        input[type="checkbox"] {
            margin-right: 8px;
            transform: scale(1.2);
        }

        button[type="submit"] {
            width: 100%;
            padding: 18px;
            background: #000000;
            color: #FFFFFF;
            border: 1px solid #000000;
            border-radius: 16px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 700;
            font-family: 'Space Grotesk', sans-serif;
            transition: all 0.2s ease-in-out;
            margin-top: 20px;
            letter-spacing: 0.01em;
        }
        
        /* Inversion hover effect */
        button[type="submit"]:hover {
            background: #FFFFFF;
            color: #000000;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        
        button[type="submit"]:active {
            transform: translateY(0);
        }

        .cancel {
            display: block;
            margin-top: 20px;
            text-align: center;
            text-decoration: none;
            color: #333333;
            font-size: 0.95rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .cancel:hover {
            color: #000000;
            text-decoration: underline;
        }

        /* Responsive Styles */
        @media (max-width: 600px) {
            body { padding: 20px; }
            form { padding: 30px 25px; }
            h1 { font-size: 1.6rem; }
        }
    </style>
</head>
<body>
    <div class="edit-wrapper">
        <h1>Edit Confession</h1>
        <form action="/<%= ADMIN_PATH %>/update" method="POST">
            <input type="hidden" name="id" value="<%= confession.id %>">
            
            <label for="text">Confession Text:</label>
            <input type="text" id="text" name="text" value="<%= confession.text %>" required>
            
            <label for="score">Score:</label>
            <input type="number" id="score" name="score" value="<%= confession.score %>" required>
            
            <label style="display: flex; align-items: center; margin-top: 20px;">
                <input type="checkbox" name="archived" <%= confession.archived ? 'checked' : '' %>> Archived
            </label>

            <label for="ipAddress">IP Address:</label>
            <input type="text" id="ipAddress" name="ipAddress" value="<%= confession.ipAddress %>">

            <label for="userAgent">User Agent:</label>
            <input type="text" id="userAgent" name="userAgent" value="<%= confession.userAgent %>">
            
            <button type="submit">Update</button>
            <a class="cancel" href="/<%= ADMIN_PATH %>">Cancel</a>
        </form>
    </div>
</body>
</html>
